# Docstring

> Follow the writing style!  
> -- Yuhao Zhu, *Gate of Heaven*

Docstring, aka documentation string, is a string literal that occurs as the first statement in a module, function, class, or method definition. It is used to document the object and can be accessed using the built-in `help()` function or by accessing the `__doc__` attribute directly.
